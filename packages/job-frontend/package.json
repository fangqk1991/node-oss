{
  "private": true,
  "name": "job-frontend",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "start": "webpack-dev-server --config webpack.dev.js",
    "build": "rm -rf dist && webpack --config webpack.prod.js",
    "build:dev": "rm -rf dist && webpack --config webpack.staging.js"
  },
  "devDependencies": {
    "@web/job-common": "workspace:^",
    "@fangcha/job-models": "workspace:^",
    "@fangcha/sso-models": "^0.6.0",
    "@fangcha/account-models": "^0.19.0",
    "@fangcha/auth-basic": "^0.9.0"
  }
}
